<h1>Burgers!</h1>

<h2>Type of burgers!</h2>

<ul>
    {{#each burgers}}
        {{#unless devored}}
            {{> burgers/burger-block devored=true}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that are devored!</h2>

<ul>
    {{#each burgers}}
        {{#if devored}}
            {{> burgers/burger-block devored=false}}
        {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="bu">Burger Name:</label>
        <input type="text" id="bu" name="name">
    </div>

    <div class="form-group">
        <label for="devor">Devored?</label>
        <br>
        <input type="radio" name="devored" value="1" checked> Devored!
        <br>
        <input type="radio" name="devored" value="0"> No Devored
    </div>

    <button type="submit">Add Burger</button>
</form>